import LoginForm from "../components/loginForm";

const LoginScreen = () => {
  return (
    <section className="h-screen flex items-center justify-center w-full mx-auto">
      <div>
        <LoginForm />
      </div>
    </section>
  );
};

export default LoginScreen;
